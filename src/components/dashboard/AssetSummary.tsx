
import React from 'react';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
import { Package, AlertTriangle, Clock, CheckCircle2, CircleChevronRight } from 'lucide-react';

const AssetSummary = () => {
  // This component is no longer used since we moved its functionality to the Index page
  return null;
};

export default AssetSummary;
